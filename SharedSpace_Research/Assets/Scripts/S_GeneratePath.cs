using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class S_GeneratePath : MonoBehaviour
{
    private void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {
        
    }

    public static void CommonTrigger(char[] location)
    {

    }

 /**
 * 
 * 1. If number of cubes less than total cubes
 *      a. Place cube randomly.
 *      b. Randomly pick up/down, left/right.
 *      c. Place a new cube.
 *      d. Check for collision.
 *           i. Replace cube if collision present.
 *      e. Goto a with new cube.
 *
 */
}
